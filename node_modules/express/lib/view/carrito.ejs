<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
</head>
<body>
    <h1>Carrito de Compras</h1>
    <% if (configuraciones.length > 0) { %>
        <ul>
            <% configuraciones.forEach(configuracion => { %>
                <li>
                    Configuración: <%= configuracion.id %> - RAM: <%= configuracion.ram %>, CPU: <%= configuracion.cpu %>, Almacenamiento: <%= configuracion.almacenamiento %>, Ancho de Banda: <%= configuracion.ancho_banda %>
                    <form action="/eliminar-del-carrito" method="POST">
                        <input type="hidden" name="idConfiguracion" value="<%= configuracion.id %>">
                        <button type="submit">Eliminar</button>
                    </form>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No hay configuraciones en el carrito.</p>
    <% } %>
</body>
</html>